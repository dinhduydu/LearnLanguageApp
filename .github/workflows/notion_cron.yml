name: Notion Daily Words

on:
  schedule:
    - cron: "0 0 * * *"   # 00:00 UTC = 07:00 sáng VN.
    - cron: "0 4 * * *"   # 11h trưa VN
    - cron: "0 5 * * *"   # 12h trưa VN
    - cron: "0 11 * * *"  # 18h tối VN
    - cron: "0 13 * * *"  # 20h tối VN
    - cron: "0 14 * * *"   # 21h tối VN
    - cron: "0 15 * * *"   # 22h tối VN
    - cron: "0 16 * * *"   # 23h tối VN
    - cron: "0 17 * * *"   # 24h đêm VN
    - cron: "0 23 * * *"   # 6h sáng VN
  workflow_dispatch:       # cho phép chạy tay trong tab Actions

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests

      - name: Run Notion Script
        env:
          NOTION_API_KEY: ${{ secrets.NOTION_API_KEY }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          DISCORD_USER_ID: ${{ secrets.DISCORD_USER_ID }}
          DISCORD_USER_HIEN_ID: ${{ secrets.DISCORD_USER_HIEN_ID }}
        run: python Notion_Remind/main.py
